# MongoDB connection URI
# Inside Docker: use mongodb://mongo:27017 (service name)
# Outside Docker: use mongodb://localhost:27017
MONGO_URI=mongodb://mongo:27017

# Total de registros a generar para CLIENTES y PRODUCTOS
# Variaciones se generan dinámicamente según tieneVariaciones
SEED_N=500000

# Registros por batch (por transacción)
SEED_BATCH=10000

# Número de workers paralelos (clientes y productos)
# Las variaciones se generan de forma secuencial leyendo BD
SEED_WORKERS=4

