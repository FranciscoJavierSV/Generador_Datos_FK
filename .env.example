# MongoDB connection URI (local docker compose)
# puedas cambiar a externa si lo deseas
MONGO_URI=mongodb://mongo:27017

# Totales (se aplican a clientes/productos/facturas si no hay valores
# específicos)
SEED_N=200000

# Valores finos opcionales (anulan SEED_N si existen)
# SEED_N_CLIENTES=100000
# SEED_N_PRODUCTOS=150000
# SEED_N_FACTURAS=50000

# Registros por batch (por transacción)
SEED_BATCH=10000

# Número de workers paralelos para clientes y productos
SEED_WORKERS=4

# -- Las variaciones se generan automáticamente según los productos
# -- Los datos de factura se crean tras facturas existentes (1-5 por factura)
